---
- name: create target directories
  file:
    name: /etc/certficates
    state: directory

- name: write tls wildcard cert
  file:
    name: "/etc/certficates/{{ inventory_hostname }}.{{ workshop_domain }}.pem"
    content: "{{ acme_cert }}"
